<?php

$config = require __DIR__ . '/pipeline_config.php';

$text = <<<'TEXT'
Documentation policy (END-STAGE ONLY): while working, do NOT update any documentation, guide, or test file after every edit — never revise a doc multiple times during a session mid-task. All documentation work happens ONCE, in a single consolidated sweep right before you declare the work done (a Stop hook enforces this with a checklist built from the session's actual edits). During work, only two things apply: read governing docs before editing (a PreToolUse gate enforces *Pattern.md docs — plus module legends and admin guides in projects that have them — before edits) and erase any temp/probe files the same turn you create them. At stage end, one consolidated pass over exactly the surfaces this project has and this session touched: (1) governing *Pattern.md docs — verify claims still true, update only what is stale, append new gotchas in the doc's existing style; (2) user legends / module guides — ONLY if this project has them, keep in sync, study existing ones first and follow their exact UI/UX grammar (grouped rows/sub-tabs, 3-4 tabs max, never loose appended lines), non-obvious nuggets only; (3) tests — ONLY if this project has a test tree: it mirrors app/source folders, ONE master test file per important class (actions/presentation/main folded into it), only meaningfully important assertions (would break behavior or silently kill a feature), never per-method files, never unconventional test files, temp tests named *Tmp* and deleted after use. If a folder matching this project's serious-directory list (pipeline_config.php serious_dirs) has no *Pattern.md walking up its tree, either create one concise doc in the established style or state in one line why the tree's existing doc already covers it. Nothing left stale, nothing unverified, no repeated edits to the same doc.
TEXT;

echo json_encode(['hookSpecificOutput' => ['hookEventName' => 'SessionStart', 'additionalContext' => $text]], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);