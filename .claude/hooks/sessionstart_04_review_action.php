<?php

$text = <<<'TEXT'
Review-action policy for this project (subagent mode, since 2026-08-30): the PostToolUse hook at .claude/hooks/post_tool_review.php is INERT whenever FATEH_REVIEW_MODE=subagent is exported (the default from ~/.claude/pipelines/models.ps1) — it never gates, it only tracks edit state for the Stop hook; do not wait on it, and its silence means nothing was reviewed automatically, not that the change is clean. Review ownership: after completing a coherent unit of work (a task or diff), before considering it done — except on the trivial lane (single file, few lines), where the Lead's own multi-pass review suffices — the session MUST spawn `claude-reviewer` via the Agent tool (no model override — inherits the driving model, so the reviewer always matches the driving agent) to independently review the change with the two lenses: correctness/security, then performance/pattern-consistency. This is mandatory and not satisfied by in-line self-review. Safe fixes from confirmed findings are applied by the Lead or a coder subagent — never by the reviewer. Max two fix cycles per finding, then surface the remainder to the user. API calls exist only for plan enrichment (kimi, plus the OpenAI refiner on max) and the end-stage dual review (REVIEWER_A + REVIEWER_B, once per stage, fixes by the Lead). If the hook does fire a block (legacy mode in not-yet-synced projects): it is an LLM judgment call — verify the issue is genuinely present in the actual code before acting; if it is a false positive, say why and change nothing; if real, fix it immediately using established patterns, never by removing them; straightforward fixes directly, architecturally significant fixes only after asking the user; stop after at most two fix cycles and surface the rest.
TEXT;

echo json_encode(['hookSpecificOutput' => ['hookEventName' => 'SessionStart', 'additionalContext' => $text]], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);